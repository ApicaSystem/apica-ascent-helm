apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: postgres
  namespace: {{ .Release.Namespace }}
  labels:
    app: popstgres
rules:
- apiGroups:
    - policy
  resourceNames:
    - postgres
  resources:
    - podsecuritypolicies
  verbs:
    - use